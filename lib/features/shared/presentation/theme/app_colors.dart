import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ─── Primarios ───────────────────────────────────────────────────────────────
  static const Color primaryDarkBlue = Color(0xFF1A3A8F);
  static const Color primaryMediumBlue = Color(0xFF2255CC);
  static const Color primaryLightBlue = Color(0xFFD6E4FF);

  // ─── Guinda IPN ──────────────────────────────────────────────────────────────
  static const Color primaryCherry = Color(0xFF6D1130);
  static const Color primaryLightCherry = Color(0xFFF2E0E7);

  // ─── Fondos ──────────────────────────────────────────────────────────────────
  static const Color backgroundWhite = Color(0xFFFFFFFF);
  static const Color backgroundLight = Color(0xFFF5F5F5);
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color screenBackground = Color.fromARGB(188, 255, 224, 174);

  // ─── Textos ──────────────────────────────────────────────────────────────────
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textPrimaryWhite = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textHint = Color(0xFFAAAAAA);
  static const Color textOnPrimary = Color(0xFFFFFFFF);
  static const Color textMuted = Color(0xFF999999);

  // ─── Estado de materias ──────────────────────────────────────────────────────
  static const Color statusAvailableBackground = Color(0xFFDFF5E3);
  static const Color statusAvailableForeground = Color(0xFF2E7D32);
  static const Color statusComingSoonBackground = Color(0xFFFFF8E1);
  static const Color statusComingSoonForeground = Color(0xFFF9A825);
  static const Color statusUnavailableBackground = Color(0xFFFFEBEE);
  static const Color statusUnavailableForeground = Color(0xFFC62828);

  // ─── Estado cronológico de ETS ───────────────────────────────────────────────
  static const Color statusTodayForeground = Color(0xFF2E7D32);
  static const Color statusTodayBackground = Color(0xFFDFF5E3);
  static const Color statusTomorrowForeground = Color(0xFF558B2F);
  static const Color statusTomorrowBackground = Color(0xFFF1F8E9);
  static const Color statusSoonForeground = Color(0xFFE65100);
  static const Color statusSoonBackground = Color(0xFFFFF3E0);
  static const Color statusFarForeground = Color(0xFFC62828);
  static const Color statusFarBackground = Color(0xFFFFEBEE);

  // ─── Chips de semestre ───────────────────────────────────────────────────────
  static const Color semesterChipSelectedBackground = Color(0xFF1A3A8F);
  static const Color semesterChipSelectedText = Color(0xFFFFFFFF);
  static const Color semesterChipUnselectedBackground = Color(0xFFE8EEF9);
  static const Color semesterChipUnselectedText = Color(0xFF1A3A8F);

  // ─── Selector de carrera ─────────────────────────────────────────────────────
  static const Color careerSelectedAccent = Color(0xFF1A3A8F);
  static const Color careerItemBackground = Color(0xFFFFFFFF);
  static const Color careerItemSelectedBackground = Color(0xFFEEF3FF);

  // ─── Búsqueda ────────────────────────────────────────────────────────────────
  static const Color searchBarBackground = Color(0xFFF0F0F0);
  static const Color searchBarBorderFocused = Color(0xFF1A3A8F);

  // ─── Divisores y bordes ──────────────────────────────────────────────────────
  static const Color divider = Color(0xFFE0E0E0);
  static const Color cardBorder = Color(0xFFEEEEEE);

  // ─── Navegación inferior ─────────────────────────────────────────────────────
  static const Color navBarActiveItem = Color(0xFF1A3A8F);
  static const Color navBarInactiveItem = Color(0xFFAAAAAA);
  static const Color navBarBackground = Color(0xFFFFFFFF);

  // ─── Notificaciones ──────────────────────────────────────────────────────────
  static const Color notificationBadge = Color(0xFFF9A825);

  // ─── Botones ─────────────────────────────────────────────────────────────────
  static const Color buttonPrimaryBackground = Color(0xFF1A3A8F);
  static const Color buttonPrimaryText = Color(0xFFFFFFFF);
  static const Color buttonSecondaryBorder = Color(0xFF1A3A8F);
  static const Color buttonSecondaryText = Color(0xFF1A3A8F);
  static const Color buttonDisabledBackground = Color(0xFFCCCCCC);
  static const Color buttonDisabledText = Color(0xFF888888);
}
