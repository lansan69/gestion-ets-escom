org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Override Flutter's default compileSdk for all plugins (required by flutter_plugin_android_lifecycle 36+)
flutter.compileSdkVersion=36
# flutter_timezone 1.0.8 sets kotlinOptions.jvmTarget="1.8" while its compileOptions
# targets Java 11 — a third-party inconsistency we cannot patch. Suppress the error.
kotlin.jvm.target.validation.mode=IGNORE
